
{% extends 'ItcAdminBundle::layout.html.twig' %}
{% block menu %}
    {# скрываем меню #}
{% endblock %}
{% block javascript %}
    {{ parent() }}    
{% endblock%}
{% block css %}
    {{ parent() }}
{% endblock %}
{% block body %}
{#{{ tinymce_init() }}#}

<div class = 'menu_sys_top_menu_bg'></div>

<div class = 'menu_sys_main_conteiner'>

<h1>Invoice\Invoice creation</h1>
<h1>Created: {{date}}</h1>
<h1>Autor: {{user}}</h1>
<hr />

<form action="{{ path('invoice_create') }}" method="post" {{ form_enctype(form) }}>
    <div class = 'fl'>
        <button class = 'menu_sys_save_button' type="submit" alt="{{ "save"|trans }}"></button>
    </div>
    <div class = 'menu_sys_upper_form'>
        <div class = 'fl'>
            {{ form_row( form.N  ) }}
        </div>
        <div class = 'fl'>
            {{ form_row( form.contract_id ) }}
            <input type="hidden" class='loadContractData'
                    data-link=".loadContractData"
                    data-route="ajax_data_from_contract" />  
        </div>
        <div class="clear"></div>
        <br class = 'clr' />
        <hr />
        <table class="contract_more_data">
            <thead>
                <tr>
                    <th>Apartment number</th>
                    <th>FIO</th>
                    <th>S all</th>
                    <th>S live</th>
                    <th>S balcony</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    
    
    
    
{#------------------------------------------------------------------------------   
        <div class="clear"></div>
        <div>
            <input type="hidden" class='search_contract_id' />
            № Contr <input type="text" class="entity_search w100pr contract_data" 
                    data-link=".loadContractData"
                    data-type-link="input"
                    data-route="ajax_search_contract"
                    data-after-search=".loadContractData" />
            <input type="hidden" class='loadContractData'
                    data-link=".loadContractData"
                    data-type-link="input"
                    data-route="ajax_data_from_contract" />  
        </div>
------------------------------------------------------------------------------#}
             
            
         {# <div class = 'fl'>
           {{ form_row( form.pdtype ) }}
        </div>
       <div class = 'fl'>
            {{ form_row( form.oa2 ) }}
        </div>
        <div class = 'fl'>
           {{ form_row( form.txt1  ) }}
        </div>
        <div class = 'fl'>
            {{ form_row( form.txt2 ) }}
        </div>
        <div class = 'fl'>
            {{ form_row( form.summa1 ) }}
        </div>
        <div class = 'fl'>
           {{ form_row( form.summa2 ) }}
        </div>
        <div class = 'fl'>
           {{ form_row( form.summa3 ) }}
        </div>
            

        #}
    </div>
        
    <br class = 'clr' />
    <hr />

{#------------------------------------------------------------------------------       
    
    <table class="records_list counters">
        <thead>
            <tr>
                <th>Name</th>
                <th>Tariff</th>
                <th>Current indicators</th>
                <th>Previous indicators</th>
                <th>Delta</th>
                <th>Zumma</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
        <tfoot>
            <tr>
                <td>All:</td>
                <td id="all"></td>
            </tr>
        </tfoot>
    </table>
    <div hidden="hidden" id='err1'>Some error</div>
        <br class = 'clr' />
    <hr />
------------------------------------------------------------------------------#}  
    {#<div class = 'menu_sys_lang_select'>

        {% for language in languages %}

            {% set lang = language %}
            {% if lang == locale %}{% set current = 'current' %}
            {% else %}{% set current = '' %}
            {% endif %}
            <div class = 'menu_sys_lang_tab {{ current }}' lang = '{{lang}}' rel = '{{lang}}'>
                {{ lang }}
            </div>

        {% endfor %}

        <br class = 'clr' />

        <div class = 'menu_sys_content_show'>

        {% for language in languages %}

            {% set lang = language %}

            {% if lang != locale %} 
                {% set hide = 'hide' %}
            {% else %}
                {% set hide = '' %}
            {% endif %}

            <div class = 'menu_sys_lang_show {{ hide }}' lang = '{{lang}}' rel = '{{lang}}'>
                 <div>
                    {% set desc = form[lang~"Translation.title"] %}
                    {{ form_label(  desc ) }}
                    {{ form_errors( desc ) }}
                    {{ form_widget( desc, { 'attr': {'class': 'menu_sys_input_edit'} }) }}
                </div>
            </div>       

        {% endfor %}

        </div>
    </div>

    <br class="clr" />
        
    {{ form_rest(form) }}#}
{{ form_rest(form) }}

</form>

</div>

{% endblock %}    
